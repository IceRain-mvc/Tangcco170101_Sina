<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tangcco.tangcco032_35">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.MyService01" />
        <service android:name=".service.MyService02" />
        <service
            android:name=".service.MyIntentService"
            android:exported="false"></service>
        <service android:name=".service.MyBindService" />
        <service android:name=".service.MyBindService01" />
        <service android:name=".service.MyBindService02" />
    </application>


</manifest>